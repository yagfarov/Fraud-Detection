# Credit Card Fraud Detection API

**Автор:** Рустам Ягфаров\

------------------------------------------------------------------------

## 📌 Обзор проекта

Проект представляет собой end-to-end ML-систему для обнаружения
мошеннических транзакций по кредитным картам (датасет Kaggle, European
cardholders).

Цель проекта --- построить production-ориентированный ML-сервис:

-   Валидация с учетом времени (time-based split)
-   Работа с сильным дисбалансом классов
-   Оптимизация порога по денежным потерям
-   Сериализация модели
-   Inference-сервис на FastAPI
-   Docker-контейнеризация

------------------------------------------------------------------------

## 🎯 Постановка задачи

Fraud detection --- задача бинарной классификации с экстремальным
дисбалансом:

-   Доля мошенничества ≈ 0.17%
-   Ошибки имеют разную стоимость

Стоимость ошибок:

-   **False Negative (FN)** → прямая потеря денег (Amount транзакции)
-   **False Positive (FP)** → операционные издержки и негативный
    пользовательский опыт

Поэтому цель --- минимизация **ожидаемых денежных потерь**, а не просто
максимизация ROC-AUC.

------------------------------------------------------------------------

## 🧠 ML-подход

### 1️⃣ Разделение данных

Использовано строгое time-based разделение:

    Train (80%)  → прошлое
    Test  (20%)  → будущее

Внутри Train:

    Train_inner (80%)
    Validation_inner (20%)

Это предотвращает temporal leakage и имитирует реальный
продакшен-сценарий.

------------------------------------------------------------------------

### 2️⃣ Работа с дисбалансом

Использованы веса классов в CatBoost:

``` python
class_weights = [1, N_negative / N_positive]
```

Это корректирует вклад классов в функцию потерь (Logloss) без искажения
распределения данных.

------------------------------------------------------------------------

### 3️⃣ Модель

Модель: `CatBoostClassifier`\
Функция потерь: Logloss\
Метрика: AUC

Метрики на Test:

-   ROC-AUC ≈ 0.977
-   PR-AUC ≈ 0.792

PR-AUC используется как более релевантная метрика при сильном
дисбалансе.

------------------------------------------------------------------------

### 4️⃣ Оптимизация порога (Business-aware)

Порог классификации выбран не по F1, а по минимизации ожидаемых денежных
потерь:

Loss = сумма FN_amount + C_fp × FP

Где: - FN стоимость = реальная сумма транзакции - FP стоимость =
фиксированная операционная стоимость

Таким образом модель становится cost-sensitive системой принятия
решений.

------------------------------------------------------------------------

## 🏗 API Архитектура

Inference-сервис реализован на FastAPI.

### Endpoint

#### `POST /predict`

Вход: признаки транзакции (V1--V28 + Amount)

Ответ:

``` json
{
  "fraud_probability": 0.034,
  "is_fraud": false,
  "threshold_used": 0.052,
  "model_version": "1.0.0"
}
```

------------------------------------------------------------------------

## ⚙ Конфигурация

Все параметры inference вынесены в `config.yaml`:

``` yaml
model_path: model/catboost_model.cbm
threshold: 0.052
version: 1.0.0
```

Это позволяет: - Изменять порог без переобучения модели - Версионировать
модель - Гибко управлять конфигурацией при деплое

------------------------------------------------------------------------

## 📂 Структура проекта

    Fraud_detect/
    │
    ├── app.py
    ├── config.yaml
    ├── model/
    │     └── catboost_model.cbm
    ├── requirements.txt
    ├── Dockerfile
    └── README.md

------------------------------------------------------------------------

## 🚀 Локальный запуск

Установка зависимостей:

``` bash
pip install -r requirements.txt
```

Запуск сервера:

``` bash
uvicorn app:app --reload
```

Документация Swagger:

    http://127.0.0.1:8000/docs

------------------------------------------------------------------------

## 🐳 Docker

Сборка образа:

``` bash
docker build -t fraud-api .
```

Запуск контейнера:

``` bash
docker run -p 8000:8000 fraud-api
```

------------------------------------------------------------------------

## 📊 Production-подход

В проекте реализованы:

-   Time-based валидация
-   Cost-sensitive threshold
-   Разделение Train / Validation / Test
-   Конфигурационное управление inference
-   Единоразовая загрузка модели

Потенциальные улучшения:

-   SHAP explainability endpoint
-   Мониторинг и логирование
-   CI/CD через GitHub Actions
-   Model registry
-   Probability calibration

------------------------------------------------------------------------

## 📈 Основные выводы

-   ROC-AUC недостаточен при сильном дисбалансе
-   PR-AUC более информативен для fraud detection
-   Порог должен определяться бизнес-стоимостью ошибок
-   Temporal drift существенно влияет на качество модели

------------------------------------------------------------------------

Проект выполнен в учебных и портфолио-целях.
